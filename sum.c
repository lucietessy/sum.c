//program to find sum of first 10 natural numbers 
#include <stdio.h>
int main()
{
    int x, sum;
    printf("\n enter the first 10 natural numbers:");
    scanf("%d",&x)
    printf("sum of first 10 natural numbers")
    if ("x=1,x<=10, x++")
    sum=sum+x;
    return 0;
}
  